export default class AutoAnchor {
    private items;
    private html;
    private match;
    private index;
    private piece;
    constructor();
    private getLinkedText(text);
    private autoAnchor(item);
}
