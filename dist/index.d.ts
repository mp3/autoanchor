export default class AutoLink {
    private items;
    private html;
    private match;
    private index;
    private piece;
    constructor();
    private getLinkedText(text);
    private autoLink(item);
}
